[pytest]
markers =
    podman: real integration tests that shell out to `podman run` against the c-sandbox image. Skipped in GitHub-hosted CI (rootless-Podman/cgroup namespace mismatch on the shared runner is an environment artifact, not an app regression) but always run against the real deploy target.
